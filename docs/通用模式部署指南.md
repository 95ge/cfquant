# 通用模式部署指南

通用模式是建议优先使用的部署方式。本文内容来自 [QMT 部署教程](QMT部署教程.md) 中的“通用模式部署教程”章节。

## 一、部署前说明

- 下载项目解压，或者通过 pip 安装后，在对应安装目录中找到我们的项目目录。
- 本文以项目下载解压作为示例，使用国金证券 QMT 作为示例，其他证券公司 QMT 按同样方式安装即可。
- 特别提醒：国泰君安和国泰海通的 QMT，需要走我们的【极致模式】。

## 二、填写 QMT 目录并自动复制核心包

在 Web 初始化或“绑定”页面填写 QMT 安装目录或 `bin.x64`。保存账号配置时，系统会自动将项目中的 `cfquant` 目录复制到对应的 `bin.x64/cfquant`；如果目录不存在或没有写入权限，页面会提示异常，修正目录后重新保存即可。

![复制 cfquant 目录](image-4.png)

## 三、安装 QMT Python 环境

打开 QMT，完成登录，并安装好相应的 Python 环境。已安装可忽略。

![安装 QMT Python 环境](image-5.png)

## 四、创建 Python 策略

打开大 QMT 里的模型研究界面，依次点击“新建策略”和“新建 Python 策略”。

![创建 Python 策略](image-6.png)

## 五、启动策略

启动我们的策略。

![启动通用模式策略](image-7.png)

## 六、重启 QMT

首次部署完成后，重启 QMT。

![重启 QMT](image-9.png)

## 七、网页端绑定账号

在网页界面中进行账号绑定。

![通用模式账号绑定](image-10.png)

## 八、确认部署成功

部署成功后的提示如下。

![通用模式部署成功](image-8.png)

## 九、接口调用测试

用我们的接口进行调用测试。

![通用模式接口测试](image-14.png)

## 十、后续支持

- 部署相对复杂一些，但延迟确实是做得比较低。
- 有不清楚的可以通过我们的首页加入我们的交流群。
- 如有远程帮忙部署指导需求，可以添加我的微信 13696119612，服务费 100 元/次。
- 如有其他二次开发需求，整体系统架构方案，多账号交易矩阵，低延迟链路，敬请联系。
